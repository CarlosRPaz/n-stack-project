
const platform = 'pc';
const region = 'us';
const battletag = 'Tip-12608';

const requests = {
    fetchProfileStats: `/${platform}/${region}/${battletag}/profile`
}

export default requests;